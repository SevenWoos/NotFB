{/* <script>
  // Handle friend request button click animations

  const acceptBtn = document.querySelectorAll('.friend-btn.accept');
  const rejectBtn = document.querySelectorAll('.friend-btn.reject');

  acceptBtn.forEach(button => {
    button.addEventListener('click', () => {
      button.classList.add('success');
      setTimeout(() => {
        button.style.display = 'none'; // Hide button after animation
      }, 1000); // Match the animation time (1s)
    });
  });

  rejectBtn.forEach(button => {
    button.addEventListener('click', () => {
      button.classList.add('success');
      setTimeout(() => {
        button.style.display = 'none'; // Hide button after animation
      }, 1000); // Match the animation time (1s)
    });
  });
</script> */}
